#!/usr/bin/node

// Get the two arguments (will be undefined if not provided)
const firstArg = process.argv[2];
const secondArg = process.argv[3];

// Print in the format: "first is second"
console.log(firstArg + " is " + secondArg);
